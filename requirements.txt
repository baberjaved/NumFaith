# NumFaith core dependencies.
# Lower-bound pins for now; exact versions are frozen in Phase 9 once the full
# stack (including detectors) is known.

pandas>=2.0
numpy>=1.24
pyyaml>=6.0
regex>=2023.0
datasets>=2.14
scikit-learn>=1.3
matplotlib>=3.7

# test
pytest>=7.4

# --- detector packages ---
lettucedetect>=0.1.4       # pretrained ModernBERT span-level hallucination detector
transformers>=4.40         # NLI / DeBERTa-MNLI entailment baseline
torch>=2.0                 # backend for transformers / lettucedetect
sentencepiece>=0.1.99      # DeBERTa-v3 tokenizer
protobuf>=3.20             # DeBERTa-v3 tokenizer
