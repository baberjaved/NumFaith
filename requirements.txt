# NumFaith dependencies — pinned to the exact versions used to produce the
# committed results (frozen in Phase 9). See README for hardware/runtime notes.

pandas==3.0.3
numpy==2.4.6
PyYAML==6.0.3
regex==2026.5.9
datasets==5.0.0
scikit-learn==1.9.0
matplotlib==3.11.0

# test
pytest==9.1.0

# --- detector packages ---
lettucedetect==0.1.8       # pretrained ModernBERT span-level hallucination detector
transformers==5.12.1       # NLI / DeBERTa-MNLI entailment baseline
torch==2.12.0              # backend for transformers / lettucedetect
sentencepiece==0.2.1       # DeBERTa-v3 tokenizer
protobuf==7.35.1           # DeBERTa-v3 tokenizer
openai==2.42.0             # LLM-judge reference detector
