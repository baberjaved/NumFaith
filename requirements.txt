# NumFaith core dependencies.
# Lower-bound pins for now; exact versions are frozen in Phase 9 once the full
# stack (including detectors) is known.

pandas>=2.0
numpy>=1.24
pyyaml>=6.0
regex>=2023.0
datasets>=2.14
scikit-learn>=1.3
matplotlib>=3.7

# --- detector packages (added in Phase 6) ---
# lettucedetect
# transformers           # NLI / DeBERTa-MNLI entailment baseline
# ragas                  # RAGAS faithfulness metric
# openai                 # LLM-judge reference
